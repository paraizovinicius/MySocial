## MySocial app

Spring boot containerized with docker. Run:

```
docker compose up
```

## Requirements: 
- The project needs a .env file (on the root dir)  to run.
- You'll need to create a bucket in Cloudfare R2